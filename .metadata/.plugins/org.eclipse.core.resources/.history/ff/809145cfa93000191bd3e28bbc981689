package Program;

import java.util.Locale;

import entities.Retangle;

public class ProrgamRetangulo {
	
	public static void main(String[] args) {
		
		Locale.setDefault(Locale.US);

		
		Retangle rect = new Retangle();
		
		System.out.println("Enter rectangle width and height: ");
		rect.width = Scan.nextDouble();
		rect.height = sc.nextDouble();
		
		System.out.println("AREA = %.2f%n" + rect.area());
		System.out.println("PERIMETER = %.2f%n" + rect.perimeter());
		System.out.println("DIAGONAL = %.2f%n" + rect.diagonal());
		sc.close();
		
	}

	
}
